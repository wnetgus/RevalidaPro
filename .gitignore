# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist_test
*.local

# Firebase CLI local cache (regenerated on every deploy)
.firebase

# Claude Code local tool config (not part of the product)
.claude/settings.json

# Artefatos gerados/temporários de sessões de importação
tmp_pdf_images
questoes_2026_1_codex.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variáveis de ambiente (NUNCA enviar para o GitHub)
.env
.env.local
.env.development
.env.production